# The main pipeline for cdc

resources:
  pipelines:
    cdc_etl:
      name: cdc_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/cdc_etl"

      libraries:
        - glob:
            include: ../src/cdc_etl/transformations/**

      #environment:
      #  dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          #- --editable ${workspace.file_path}
